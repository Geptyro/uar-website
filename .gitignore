node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
__pycache__/
static/replays/*.SC2Replay

# Share cards, drawn from src/lib/data by scripts/build-og.ts as the first
# pass of `npm run build`. Generated, so never committed — 500-odd PNGs that
# would otherwise be re-added to history on every data refresh.
static/og/
