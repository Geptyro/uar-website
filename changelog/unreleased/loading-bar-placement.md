---
title: The page loading bar sits under the top bar
type: improvement
area: site
impact: minor
---
The loading bar now fills in along the bottom edge of the top bar instead of
floating at the very top of the window, so it reads as part of the page
chrome — and it follows the taller top bar on narrow screens.
