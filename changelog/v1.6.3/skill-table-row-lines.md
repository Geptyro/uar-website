---
title: Row lines on skill tables line up again
type: fix
area: wiki
impact: minor
---

On class pages, the per-level table under each skill drew the line under a row short in the name column, so the rows looked cut. The name column now fills each row like the level columns beside it. The skills table at the top of a guide had the same cell and got the same fix.
